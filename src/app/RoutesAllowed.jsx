import { Switch, Route } from "wouter";

import Home from "../pages/Home";

const void_container = ({ children }) => (children);

const routes_allowed = [
    {
        component: 
    }
];

export default function RoutesAllowed() {

    return (
        <Switch></Switch>
    )

}